# CLAUDE.md — omniskill

OmniSkill is unified skill infrastructure for AI agents in Go: Skill/Tool/Role abstractions, MCP server and client bridging, and pack-based skill distribution (registry, loader, installer, ClawHub). Specs live in `docs/specs/` (PRD, TRD, PLAN, ROADMAP); read them before implementing.

## PRISM Control

This repo's roadmap items are tracked in [prism-control](https://github.com/ProductBuildersHQ/prism-control). Use `prismctl work ready --repo github.com/plexusone/omniskill` to find claimable work, and carry the `Refs: RMI-OMNISKILL-<NNN>` trailer on every commit.
